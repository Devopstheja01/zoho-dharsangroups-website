[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "18"
  
# Ensure images are included
[[redirects]]
  from = "/images/*"
  to = "/images/:splat"
  status = 200
