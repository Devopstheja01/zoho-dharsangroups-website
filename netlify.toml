[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# Force redeployment by adding environment variable
[build.environment]
  NODE_VERSION = "18"
